# SkillBuoy 项目约定

本文件适用于整个仓库，补充执行环境和用户已有的指导规则。明确的用户要求优先；已授权范围内继续推进，不重复确认，也不自行扩大范围。

## 项目定位

SkillBuoy 原地管理已安装的 Agent Skills：扫描、识别来源、检查变化、审阅 Diff、受控升级与回滚。

- 当前交付物是 macOS/Linux 方向的本地 CLI；已完成的主机验证以实际测试结果为准。
- 保持单包项目和本地文件存储。Web 面板、Vite、数据库、自动升级或新安装器适配需要相应任务授权。
- 使用 Node.js 24、TypeScript 7 和 `package.json` 固定的 pnpm 版本。不要切换运行时或修改全局工具配置。

## 代码入口与边界

| 位置                        | 职责                                       |
| --------------------------- | ------------------------------------------ |
| `src/cli/`                  | 参数、确认、终端与 JSON 输出               |
| `src/core/`                 | 扫描、检查、计划、更新、Diff、恢复         |
| `src/adapters/`             | 只读导入第三方安装器元数据                 |
| `src/infra/`                | Git、文件系统、快照、状态与锁              |
| `src/schemas/`              | 数据类型与 Zod 校验                        |
| `tests/integration.test.ts` | 临时目录、本地 Git 仓库和 CLI 进程集成测试 |

核心逻辑返回结构化数据，不读取终端输入、不打印表格、不依赖 HTTP。保持 `planUpgrade` 和 `applyUpgrade` 分离，后续面板复用核心 API。

## 开发与验证

```sh
pnpm install --frozen-lockfile --ignore-scripts
pnpm format
pnpm check
```

- `pnpm check` 依次执行格式检查、Oxlint 类型感知检查、源码与测试类型检查、构建和集成测试。
- `tsconfig.json` 检查源码和测试但不输出文件；`tsconfig.build.json` 仅构建源码到 `dist/`。
- `pnpm test` 会先构建。单独运行某个 Vitest 用例前也必须先构建，因为测试会启动 `dist/cli/index.js`。
- 新增依赖使用 pnpm，保留单一 `pnpm-lock.yaml`。不要混入 npm/Bun 锁文件，或为安装依赖而静默放宽校验策略。
- 保持 Oxlint/Oxfmt 配置简洁。局部规则例外必须解释具体原因，不能通过整体关闭检查掩盖问题。
- 测试使用临时目录和独立状态目录，不接管、升级或回滚开发者真实的 Skills，不依赖 GitHub 在线状态。
- 测试优先覆盖公开核心 API 和 CLI 行为。代码或工具链改动运行 `pnpm check`；纯文档改动核对格式、链接及命令示例。

## Code Review Rules

- 来源必须来自明确绑定或可解释的安装记录；不能通过同名 Skill 或业务项目的父级 Git remote 猜测来源。不同类型的导入哈希不可直接比较。
- 保留上游基线、本地内容、本次上游目标三者的区别。未知基线不等于本地干净；首次同步必须明确选择单个安装。
- 实际更新必须使用已审阅计划中的 commit 和快照。执行前重新核对来源、基线、本地内容、真实路径及全部入口链接；`--yes` 只省略交互确认。
- 同一真实目录只写入一次，独立副本分别管理。系统、插件和 Git 工作区安装禁止直接接管；批量更新跳过未接管、来源未知、基线不明、本地修改和固定版本等条目。
- 不执行 Skill 内容、安装其依赖、调用外部安装器更新命令或在安装目录执行 `git pull`。Git 使用参数数组；缓存 fetch 不启动后台自动维护。
- 保留路径、链接、文件类型和资源限制。替换目录前保存完整快照和恢复记录；损坏状态必须报错，不能静默重置。
- 恢复与回滚不能覆盖后续修改。检测到冲突时保留安装、备份和恢复记录，明确报告阻塞原因。
- 涉及文件写入、路径校验、并发、权限或恢复的修改，完成测试后追加一次代码审查。普通目录替换不能声称跨目录原子性或活动 Agent 无缝读取。

## Git、文档与公开发布

- 开始修改和推送前核对机器、工作目录、Git 根目录、分支、状态及差异；展示 remote 前脱敏。
- 新工作分支使用 `feat/`、`fix/`、`chore/`、`refactor/` 或 `ci/`，不要使用 `codex/`。
- 提交、推送和发布遵循用户的明确授权。不要自动暂存用户已有改动；用户要求先审阅时，等审阅通过再执行相应提交或远端操作。
- 按当前授权创建或修改文档；核对 `Readme.md` 与实际 CLI，不能把规划功能写成已实现能力。
- 公开仓库不能包含凭证、个人安装清单、真实 Skill 内容、状态目录、缓存或快照。示例使用占位值，测试使用临时数据。
- 本项目采用 MIT，第三方依赖和 Skills 保留各自协议。引入外部代码时检查许可证并保留所需声明。
- `private: true` 用于防止误发 npm，不决定 GitHub 仓库可见性。公开 GitHub、发布 npm、创建 Release 和更换协议是不同的授权范围。
